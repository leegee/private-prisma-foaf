# Testing Prisma

  # docker up postgres db
  npm run db:start

  # build scheme, populate db, create erd, jest
  npm run init



# TODO
* Allows tests to run in parallel
* Playwrite
* Percy
* Mermaid